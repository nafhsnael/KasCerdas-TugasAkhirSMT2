# TODO - Clean Admin Frontend

## Goal
Di folder `admin/src copy/` hanya tersisa menu: **Dashboard, Users, Reports, Maintenance, Profile**. Hapus yang lain.

## Steps
- [ ] Validasi daftar halaman yang masih direferensikan dari `admin/src copy/App.jsx`.
- [ ] Hapus file/pages/components yang tidak direferensikan dari `App.jsx`.
- [ ] Hapus komponen yang hanya dipakai oleh file yang sudah dihapus.
- [ ] Pastikan `npm run build` (root) sukses.
- [ ] Pastikan `vite` dev admin tidak error (kalau ada command admin).

